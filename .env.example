# Local build configuration (non-secret)
#
# Copy to `.env` to override defaults in the Makefile for local builds.
# This file is NOT used in GitHub Actions; it only affects `make` locally.
# Do NOT add secrets here.

# Organization/registry
#ORG=rulehub
#REG=ghcr.io/${ORG}

# Tag refs (used as image tags, e.g., latest or v1.2.3)
#BASE_REF=latest
#POLICY_REF=latest
#CHARTS_REF=latest
#FRONTEND_REF=latest

# Repository path containing base/, policy/, charts/, frontend/ (defaults to repo root)
#IMAGES_REPO=${PWD}

# Example: build base images with a custom tag
#BASE_REF=dev-local
